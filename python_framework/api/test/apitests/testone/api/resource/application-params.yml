print-status: False

api:
    server:
        port: 5010
        host: localhost
        base-url: /local-test-api

swagger:
    schemes: [${api.server.scheme}]
    host: ${api.server.host}:${api.server.port}
